# UI
streamlit==1.31.1

# --- Pre-v1 LangChain family (consistent 0.3.x set)
langchain==0.3.27
langchain-core==0.3.80
langchain-community==0.3.31

# --- Hugging Face integration (pre-v1 matching line)
langchain-huggingface==0.3.1

# --- Tokenizers (ensure compatible >=0.19)
tokenizers==0.19.1

# --- Transformers (must be >=4.41 for tokenizers>=0.19 compatibility)
transformers==4.41.0

# --- Sentence Transformers (compatible with transformers>=4.41)
sentence-transformers==2.2.2

# --- Vector DB (faiss wheel compatible with py3.10/py3.11)
faiss-cpu==1.13.0

# --- PDF loader
pypdf==4.0.1

# --- Keras 3 compatibility shim
tf-keras==2.17.0


